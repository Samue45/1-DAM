package Programming.ReviewExercises.Part1;

public class TheoryExercises {

    //Explica brevemente qué es una clase en programación orientada a objetos.
    /*
    * Una clase es un tipo de dato que se comporta como un molde, es decir, funciona como una plantilla
    * que contiene unos atributos y métodos generales los cuales van a ser copiados para crear múltiples objetos
    * */

    //¿Cuál es la diferencia entre los operadores ++ y -- cuando se usan antes y después de una variable? Da un ejemplo para cada caso.
    /*
    * l operador "++" nos permite incrementar de uno en uno el valor de la variable. Mientras que el operador "--" nos permite decrementar de uno en uno.
    * Dependiendo del lugar donde se coloque pueden darse los siguientes casos:
    * 1º Caso -> ++variable (En este caso primero incrementar y luego accede al valor de la variable)
    * Ejemplo: int a = 5;
    *          ++a; // Si lo imprimo por consola saldría 6
    *
    * 2º Caso -> variable++ (En este caso primero accede al valor de la variable y luego incrementar)
     * Ejemplo: int a = 5;
     *          a--; // Si lo imprimo por consola saldría 5 y luego incrementaría
     *
     * El operador actuaría igual pero decrementando
    * */

    //¿Qué es el tipo de dato float y cuándo sería conveniente utilizarlo?
    /*
     * Se trata de un tipo de dato primitivo que representa números enteros, su rango es menor al del tipo de dato int.
     * Interesa cuando trabajo con números enteros de un rango más pequeño que los int pero mayores a los bytes y shorts
     * */
}
